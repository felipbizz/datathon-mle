service: "service:svc"  # Aponta para a variável 'svc' no arquivo 'service.py'
description: "API para classificação de candidatos."
labels:
  owner: meu-time
  project: classificacao-candidados
include:
  - "*.py"  # Inclui todos os arquivos Python do diretório (como service.py)
# Opcional: se você tivesse arquivos extras (ex: .txt, .csv) para incluir:
#  - "dados_extras/*"
python:
  packages:
    # Liste aqui as bibliotecas EXATAS que seu código precisa
    - scikit-learn # <--- Ajuste a versão se necessário, ex: scikit-learn==1.2.2
    - pandas>=1.0 # <--- Exemplo de restrição de versão
    # Adicione outras dependências do seu modelo ou código
    # - numpy
    # - xgboost
    # - pytorch
    # - tensorflow
# Opcional: Se precisar de dependências do sistema (ex: apt packages no Debian)
# system_packages:
#   - libgomp1

# Opcional: Define modelos que este Bento usa (já salvos com save_model.py)
# Ajuda o BentoML a empacotar o modelo correto
models:
  - classificador_de_candidatos:v1 # <--- Use a tag exata do modelo